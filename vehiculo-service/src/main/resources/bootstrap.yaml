spring:
  application:
    name: vehiculo-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    # url: jdbc:mysql://localhost:3306/bdmicros
    url: jdbc:postgresql://localhost:5432/gestion-auto-db
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
